# Implementation Plans

Generated by the improve skill on 2026-09-16. Execute in the order below unless
dependencies say otherwise. Each executor: read the plan fully before starting,
honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | `oca notify` — one-shot reminders via systemd/cron | P1 | M | — | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale — finding fixed independently or approach abandoned)

## Dependency notes

- No dependencies; 001 is standalone.

## Findings considered and rejected

- **Honoring VALARM `TRIGGER` in `oca notify`**: deferred — per-event alarm
  offsets are a follow-up once the basic window+dedup mechanism lands (see the
  plan's maintenance notes).
- **Notifying optionNotes due tasks**: deferred — tasks have date granularity,
  not times; folding them into a minute-level window produces noise.
- **All-day event reminders**: deferred — midnight notifications are noise; a
  configurable morning hour is the right shape if requested.
- **Running a notify daemon**: rejected — violates the family's "no daemon"
  rule; systemd user timers/cron are the supervisor.
